import { AcpPlaceholderLayoutDirective } from './acp-placeholder-layout.directive';
import { ElementRef } from '@angular/core';

describe('AcpPlaceholderLayoutDirective', () => {
  it('should create an instance', () => {
    const directive = new AcpPlaceholderLayoutDirective(null);
    expect(directive).toBeTruthy();
  });
});
